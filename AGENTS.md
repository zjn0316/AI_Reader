# AGENTS.md — AI 开发协作规则

本文件是 AI 开发助手的强制行为准则，遵循以下两条核心规则完成所有编码任务。

## 规则 1：每次改动后必须创建 Git commit

- 每完成一个功能或修复，立即执行 `git add` 并 `git commit`，写入清晰的中文或英文 commit 信息。
- 便于随时回滚与追踪，禁止把多处不相关改动堆叠进同一个 commit。

## 规则 2：每次改动后必须编写/更新测试，交付前确保所有测试通过

- 新增或修改功能时必须补充对应的单元测试（Vitest + React Testing Library）。
- 交付前运行 `npm run test`，确认全部测试通过后再提交。
- 禁止提交带失败测试的代码。
